{% for breadcrumb_element in breadcrumb_list %}
    <a href="{{ breadcrumb_element.link }}">{{ breadcrumb_element.title }}</a>
    {% if loop.last == false %}
        <span class="sf-breadcrumb-sep">»</span>
    {% endif %}
{% endfor %}
