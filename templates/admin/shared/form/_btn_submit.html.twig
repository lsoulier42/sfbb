{% block submit_widget %}
    <button type="submit" class="btn btn-primary btn-icon">
        <i class="fa-solid fa-check"></i>
        {{ label|default('global.label.confirm'|trans) }}
    </button>
{% endblock %}
