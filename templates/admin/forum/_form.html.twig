{% form_theme form 'admin/shared/form/_btn_submit.html.twig' %}
{{ form_start(form) }}
{% embed 'admin/shared/_card.html.twig' with {
    'title': forum is defined ? 'forum.title.edit'|trans({'%title%': forum.title}) : 'forum.title.new'|trans,
    'icon': 'fa-pen-to-square'
} %}
    {% block card_actions %}
        <a href="{{ path('admin_forum_index') }}" class="btn btn-outline-secondary btn-sm btn-icon">
            <i class="fa-solid fa-arrow-left"></i> {{ 'global.label.return'|trans }}
        </a>
    {% endblock %}
    {% block card_body %}
        <div class="row g-3">
            <div class="col-12 col-md-6">
                {{ form_row(form.category) }}
            </div>
            <div class="col-12 col-md-6">
                {{ form_row(form.title) }}
            </div>
            <div class="col-12">
                {{ form_row(form.subTitle) }}
            </div>
            <div class="col-12 d-flex gap-2 mt-4">
                {{ form_widget(form.submit, {'label': forum is defined ? 'global.label.update'|trans : 'global.label.create'|trans}) }}
                <a href="{{ path('admin_forum_index') }}" class="btn btn-outline-secondary btn-icon">
                    <i class="fa-solid fa-xmark"></i> {{ 'global.label.close'|trans }}
                </a>
            </div>
        </div>
    {% endblock %}
{% endembed %}
{{ form_end(form) }}
